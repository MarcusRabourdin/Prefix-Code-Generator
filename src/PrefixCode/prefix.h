#ifndef PREFIX_H
#define PREFIX_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "../tree/tree.h"

char *decode(char *encoded, char *key_value[], char *letter);

#endif // ! PREFIX_H
